{
  "name": "vaultguard",
  "version": "1.0.0",
  "private": true,
  "description": "VaultGuard — Enterprise Password Manager",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "npm@10.2.4"
}
